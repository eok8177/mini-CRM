{# empty Twig template #}
{% set currentPath = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) %}
<div class="panel panel-primary filterable">

<table class="table table-hover">
    <thead class="panel-heading">
        <tr>
            <th><i class="fa fa-sort" aria-hidden="true"></i>Id</th></a>
            <th>Action</th>
            <th><i class="fa fa-sort" aria-hidden="true"></i>firstName</th>
            <th><i class="fa fa-sort" aria-hidden="true"></i>lastName</th>
            <th>Phone</th>
            <th><i class="fa fa-sort" aria-hidden="true"></i>Email</th>
        </tr>
    </thead>
    <tbody>

        {% for key, guest in guests %}
            <tr>
                <td>{{ guest.id }}</td>
                <td>
                        <div class="btn-group btn-group-sm">
                                <a href="{{ path('sadmin_guest_edit', {'id' : guest.id}) }}" class="btn"><span class="fa fa-pencil" aria-hidden="true"></span></a>
                                <a href="{{ path('sadmin_guest_delete', {'id' : guest.id}) }}" class="btn delete"><span class="fa fa-trash" aria-hidden="true"></span></a>
                        </div>
                </td>
                <td>{{ guest.firstName }}</td>
                <td>{{ guest.lastName }}</td>
                <td>{{ guest.phone }}</td>
                <td>{{ guest.email }}</td>
            </tr>
        {% endfor %}


    </tbody>
</table>
</div>